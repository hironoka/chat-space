-# .flash-message
-#   - flash.each do |key, value|
-#     %content_tag :div, value, class: "alert alert-#{key}"
-# .flash-message
-#   - flash.each do |key, value|
-#     %div{ class: "alert alert-#{key}" }
.flash-message
  -flash.each do |key, value|
    %div{ class: "alert alert-#{key}" }
      = value
